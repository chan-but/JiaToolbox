<Window x:Class="ToolBoxApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToolBoxApp"
        mc:Ignorable="d"
        Title="嘉工具箱" Height="720" Width="1200" MinHeight="500" MinWidth="900"
        Background="{StaticResource BgPrimary}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResizeWithGrip" AllowsTransparency="False">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="4" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 自绘标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource BgCard}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🧰 嘉工具箱" FontSize="13" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="14,0,0,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="MinimizeBtn" Content="&#xE921;" Style="{StaticResource TitleBarButton}" Click="Minimize_Click"/>
                    <Button x:Name="MaximizeBtn" Content="&#xE922;" Style="{StaticResource TitleBarButton}" Click="Maximize_Click"/>
                    <Button x:Name="CloseBtn" Content="&#xE8BB;" Style="{StaticResource TitleBarCloseButton}" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 顶部功能栏 -->
        <Border Grid.Row="1" Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="24,12">
            <Grid>
                <TextBlock Text="🧰 嘉工具箱" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="⚙️ 设置" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="Settings_Click"/>
                    <Button Content="📥 导入配置" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="ImportConfig_Click"/>
                    <Button Content="📤 导出配置" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="ExportConfig_Click"/>
                </StackPanel>
            </Grid>
        </Border>


        <TabControl x:Name="MainTabControl" Grid.Row="2" Background="{StaticResource BgPrimary}" BorderThickness="0" Margin="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="{StaticResource BgCard}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                    <Setter Property="Padding" Value="16,10"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,1" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource BgPrimary}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Tab 1: 工具箱 -->
            <TabItem Header="📦 工具箱">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SearchBox" Grid.Column="0" Height="36" VerticalContentAlignment="Center"
                                 TextChanged="SearchBox_TextChanged"/>
                        <Button Grid.Column="1" Content="➕ 添加工具" Style="{StaticResource PrimaryButton}" Margin="12,0,0,0" Click="AddTool_Click"/>
                        <Button Grid.Column="2" Content="🔍 扫描" Style="{StaticResource SecondaryButton}" Margin="12,0,0,0" Click="ScanTools_Click"/>
                    </Grid>

                    <StackPanel Grid.Row="1" x:Name="CategoryPanel" Orientation="Horizontal" Margin="0,0,0,16"/>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ToolsItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Tab 2: 开机自启动 -->
            <TabItem Header="🚀 开机自启动">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="维护开机自启动程序清单：检测是否已安装到指定目录 -&gt; 未安装则自动下载并静默安装 -&gt; 登记到开机启动项。清单可随时增删改。"
                               Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                    <Grid Grid.Row="1" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox x:Name="LoginSyncCheckBox" Grid.Column="0" Content="开机登录时自动后台同步（无需打开本程序）" VerticalAlignment="Center"
                                  Checked="LoginSyncCheckBox_Changed" Unchecked="LoginSyncCheckBox_Changed"/>
                        <Button Grid.Column="1" Content="🔄 立即同步全部" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="SyncAllAutoStart_Click"/>
                        <Button Grid.Column="2" Content="➕ 添加自启动项" Style="{StaticResource PrimaryButton}" Click="AddAutoStartItem_Click"/>
                    </Grid>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="AutoStartItemsControl"/>
                    </ScrollViewer>

                    <Border Grid.Row="3" Style="{StaticResource CardBorder}" Margin="0,16,0,0" Height="140">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="SyncLogText" Foreground="{StaticResource TextSecondary}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" Text="同步日志将显示在这里..."/>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
